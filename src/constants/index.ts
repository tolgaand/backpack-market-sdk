const BASE_API_URL = "https://api.backpack.exchange";

export const API_VERSION = "v1";

export enum BASE_URL {
  API = `${BASE_API_URL}/api/${API_VERSION}`,
  WAPI = `${BASE_API_URL}/wapi/${API_VERSION}`,
}
